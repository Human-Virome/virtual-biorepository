* SRA - stick to predefined fields only. "not collected" value NOT okay
* BioSamples - any custom fields. "not collected" value IS okay

* Kemi is working from the MIMS packages/templates; all human-like ones combined
https://www.ncbi.nlm.nih.gov/biosample/docs/packages/MIMS.me.human-associated.6.0/



5/5/2026
  - mode_of_administration: Allow multiple values? How to map to multiple prescriptions?
  - lifetime_vaccinations and seasonal_vaccinations: Ok to use CVX IDs?
  - sample_transit_duration and samp_store_dur: What units? -> hours
  - lab: CV needed.



5/14/2026
  - library_aliqout_id: Unclear what this is. Apply uid rules? - Yes
  - sequencing_instrument_model: Far fewer CV entries than sequencing_platform.

combine abx systemic and topical
apply colon seps as optional DB ID addon/suffix



5/27/2026

  - animal_exposure: No way to indicate both or neither. Redundant with exposure_animal_type.
     -> switched to yes/no field
     
  - files.library_aliqout_uid is redundant with libraries' library_aliquot and parent_library_uid.
     -> removed field
  
  - SRA:title will be auto-generated by NCBI.
     - "[instrument_model] [library_layout] sequencing of [BioSample Organism/ID]"
     - E.g. "Illumina HiSeq 2500 PAIRED sequencing of SAMN02335374"
     - will not manually override.
     
  - how to derive SRA:library_strategy, SRA:library_source, and SRA:library_selection from technique?
     - need a limited CV for technique and hard-coded mappings to other attributes.
     
  - add biosample fields?
     - collection_date, a required field
     - host_tot_mass (weight + weight_units)
     - smoker (yes/no)
     - travel_out_six_month, new data capture
     - host_sex, in addition to custom host_sex_at_birth

  - bioproject_id is on both libraries and files.
     - Will they ever be different? If not, only have in one table.
     - add to samples table? (biosample bioproject_accession attribute)
  
  - add extraction_lab, lib_prep_lab, and sequencing_lab to allow correcting for confounding factors
     - or does `lab` already capture this? "where the sample was collected and/or processed", metadata responsibility

  
  - confirm SRA uploads will always be:
     - merged reads
     - fastq files
     - scrubbed_sequence_reads
     
  - use HVP IDs (e.g. hvp:s-1b3d5f) or date depositor's UIDs when submitting data to NCBI?
     


# In case of git complaints:
git push origin main --force
